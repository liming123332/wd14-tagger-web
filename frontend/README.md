# WD14 反推 Web 前端

基于 WD14 模型的图片反推打标系统前端（Vue 3 + Vite + TypeScript + Naive UI）。

## 开发
```bash
npm install --registry=https://registry.npmmirror.com
npm run dev      # http://localhost:5173（代理 /api → localhost:8000）
npm run build    # 输出到 dist/
```

后端 API 见 `../backend/`。
