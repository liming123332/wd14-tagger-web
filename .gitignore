__pycache__/
*.pyc
.venv/
.pytest_cache/
node_modules/
# frontend/dist/ 不忽略：纳入 git，整合包 git 更新时同步前端（用户无 node 无法 build）
models/
data/
*.part
*.onnx
.DS_Store

# pack_portable 下载缓存（重建时复用 CUDA wheel，省 600MB 重下，不入 git）
_dl/
